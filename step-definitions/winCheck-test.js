// This requires Game, Board, sleep, $ and $$ as globals
// (do this in each step-definition file in this project)
//Runs test for board Constructor
require('./_include-all')();

module.exports = function () {

  let playerOne;
  let playerTwo;

  this.Given(/^that the two players have already played/ ^ function () {

  });

  this.Then(/^a method should return an object/ ^ function () {

  });

  this.Then(/^this object should set the value of the winner to a value of 1 or 2/ ^ function () {

  });

  this.Then(/^a combo should be also be set which is an inner array of 4 arrays/ ^ function () {

  });

  this.Then(/^every array has a position on the board in the form (rowNumber, colNumber)/ ^ function () {

  });


}